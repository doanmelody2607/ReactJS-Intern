import React from 'react';
import PropTypes from 'prop-types';

Edit.propTypes = {
    
};

function Edit(props) {
    

    return (
        <div>
            Edit
        </div>
    );
}

export default Edit;