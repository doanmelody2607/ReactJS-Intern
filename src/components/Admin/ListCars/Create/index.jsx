import React from 'react';
import PropTypes from 'prop-types';

Create.propTypes = {
    
};

function Create(props) {
    return (
        <div>
                Create
        </div>
    );
}

export default Create;